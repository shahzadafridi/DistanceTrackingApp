<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/testCloud</item>
        <item name="colorPrimaryDark">@color/testCloud</item>
        <item name="colorAccent">@color/testBlack</item>
    </style>

</resources>
